# ポートフォリオLP 設計仕様書

> 用途: クラウドワークス／ランサーズの提案文・プロフィールに貼る外部リンク先
> 実装: Claude Code（静的サイト / GitHub Pages）
> リポジトリ想定: `wada3333/portfolio`（公開URL: https://wada3333.github.io/portfolio/）

---

## 0. このLPの唯一の仕事

**「この人に発注して大丈夫か」を90秒で判断させる。**

営業LPではない。提案文を読んだクライアントが「もう少し知りたい」でクリックしてくる、その1回の閲覧で不安を消す。
したがって優先順位は以下の順で固定する。

1. 何ができる人か（できないことも含む）
2. 実際に納めたものの証拠
3. どう進むか（発注後の見通し）
4. 稼働条件（夜間・週末／チャット限定）

4を隠さないのが最大の設計判断。日中の同期対応を求めるクライアントを入口で外すことで、成約後の事故と低評価を防ぐ。ミスマッチ排除はコンバージョンの一部として扱う。

---

## 1. デザイン方針

### コンセプト: 結線図

題材が「手作業の工程を自動で繋ぐ」であること、経歴が通信インフラ（NTTグループ／工事担任者AI・DD総合種）であることから、**配線図・結線図**を全体のモチーフにする。装飾ではなく、情報構造そのものを線で表す。

- セクション間は余白ではなく「1本の縦線」で繋ぐ（左端に細い導線が通り、各セクションで分岐する）
- 実績カードは箱ではなく、導線から枝分かれした「端子」として配置する
- 角丸は導線の折り返し部分にのみ使い、コンテンツブロックには使わない

### やらないこと（生成物っぽさの排除）

- クリーム地＋大きめセリフ＋テラコッタのアクセント → 使わない
- 全部を同じ角丸カードに切り刻むレイアウト → 使わない
- 見出しの上のトラッキングを広げた英字ラベル、`・`区切りのメタ情報 → 使わない
- ボタン文言末尾の `→` → 使わない
- セクションごとのフェードイン → 使わない（モーションはヒーローの1回だけ）

### カラートークン

| 名前 | 値 | 用途 |
|---|---|---|
| `--ink` | `#101826` | 本文・見出し（濃紺。黒は使わない） |
| `--paper` | `#EDF0F2` | 背景（寒色寄りの淡いグレー） |
| `--paper-deep` | `#DDE3E8` | 交互セクション背景・罫線の面 |
| `--line` | `#1F5FBF` | 導線・結線図・リンク（LANケーブルの青） |
| `--terminal` | `#E8B10D` | 端子・現在地・CTA下線（山吹） |
| `--mute` | `#63707E` | 補足・キャプション |

`--line` と `--terminal` は同一画面内で近接させない。青が「経路」、黄が「到達点」を表す約束にする。グラデーションは使わない。

### タイポグラフィ

日本語主体のページなので、日本語書体を主役にする。

- 見出し・本文（和文）: `Zen Kaku Gothic New`（400 / 700）
- 数字・英字: `Inter Tight`（500 / 700、`font-variant-numeric: tabular-nums`）

数字専用のモノスペースは使わない。数値は本文書体のタブular数字で組み、桁を揃える。

スケール（1.25倍 / 16pxベース）:

```
--fs-hero   : clamp(2.0rem, 5.2vw, 3.4rem)  / line-height 1.28 / weight 700
--fs-h2     : 1.75rem / 1.35 / 700
--fs-h3     : 1.25rem / 1.5  / 700
--fs-body   : 1.0rem  / 1.85 / 400
--fs-small  : 0.875rem/ 1.7  / 400
```

和文本文は左揃え固定。中央揃えはヒーローの見出しでも使わない。

### 行長（36〜40文字はデスクトップ前提）

**「1行36〜40文字」はデスクトップでの指定であり、狭い幅では達成できない。** `max-width: 34em` は16pxで **544px** に相当し、本文ブロックがそれ以上の幅を取れるとき — つまりビューポート幅がおよそ **605px以上** のときにだけ効く。それ未満では行長を決めているのは34emではなく**ビューポート幅**である。

375px での実測（本文の幅319px）:

| 箇所 | 文字サイズ | 1行目の文字数 |
|---|---|---|
| ヒーローのリード | 16px | 20文字 |
| CTA本文 | 16px | 16文字 |
| できること 説明文 | 14px | 32文字 |
| 納めたもの 本文 | 14px | 22文字 |

したがって狭い幅で `max-width` をいじっても行長は変わらない。36〜40文字は **605px以上でのみ満たすべき目標**として扱い、それ未満では「横スクロールを出さない」「文節の途中で改行しない」を優先する。

### ヒーロー見出しの改行

`<br>` で2行に固定しない。狭い幅で「工程を、」だけの行が孤立するため。

実測（`--fs-hero` の下限 2rem = 32px 時）:

| 幅 | 見出しに使える幅 | `手で繰り返している工程を、` の幅 |
|---|---|---|
| 375px | 335px | 2rem で 420px / 1.75rem で 368px / 1.625rem で 342px / **1.5rem で 316px** |

つまり375pxで2行に収めるには見出しを **1.5rem** まで落とす必要があり、それでは `--fs-h2`（1.75rem）を下回って見出しの階層が壊れる。したがって**文字サイズは下げず、折り返す位置を制御する**。

- 文節（`手で繰り返している` / `工程を、` / `1本の線に` / `置き換えます。`）を `white-space: nowrap` の単位に分け、折り返しが必ず文節の切れ目で起きるようにする
- 2行組みが余裕をもって収まる **512px 以上** でのみ、仕様書どおりの2行に固定する（節の `<span>` を `display: block` にする）
- 511px以下は3行を許容する: `手で繰り返している` / `工程を、1本の線に` / `置き換えます。`
- 336px未満は最長の文節（約291px）自体が収まらないので `nowrap` を外す（横スクロールを出さないことを優先）

`word-break: auto-phrase` はChromeでしか効かないため、これに頼らず `nowrap` の単位で担保する。

### レイアウト

```
┌──────────────────────────────────────┐
│ [固定ヘッダー: 名前 / 現在受付中 ●]        │
├──┬───────────────────────────────────┤
│  │  HERO                             │
│導│  見出し（2行）                      │
│線│  ─────────────────────           │
│  │  [結線図: 手作業6工程 → バイパス1本]   │
│  │  稼働条件1行                        │
│  ├───────────────────────────────────┤
│  ├─● できること（3領域 / 横並び→縦積み）  │
│  ├───────────────────────────────────┤
│  ├─● 納めたもの（ケース3本 / 縦積み）      │
│  ├─● 公開デモ（大きめ図版2枚）            │
│  ├─● 技術と資格                        │
│  ├─● 進め方（5ステップ・実際に順序がある）  │
│  └─● 依頼する                          │
└──┴───────────────────────────────────┘
```

左の導線は`position: sticky`ではなく、単純な擬似要素の縦線。**幅によらず常に表示する。**

| | 768px以上 | 767px以下 |
|---|---|---|
| 縦線の位置 | ページ左端から `--gutter`（32px） | ページ左端から **10px**（帯の中） |
| h2・h3・本文の左端 | `--gutter` + `--rail`（76px） | **36px**（`--rail` を 2.25rem に切り替え） |
| 端子 | 縦線から角丸のエルボーで分岐し、枝の先にドット | 横線＋ドットに簡略化（枝15px・ドット6px） |

767px以下でも**h2・h3・本文の左端はすべて 36px で揃える**。端子だけが左の帯に張り出す形で、PCと同じ構造を保つ。左端を28pxにすると枝が7pxしか取れず「導線から分岐している」ように見えないため、36pxを下限とする。

角丸は導線の折り返し（エルボー）だけに使う。767px以下は枝が15pxしかなく角丸を入れる余地がないので、直線の横線にする。

### モーション

**ページロード時に1回だけ。** ヒーローの結線図で、上段に並んだ手作業6工程が薄く表示された後、下側にバイパス線が左から右へ0.9秒で引かれ、右端の端子が`--terminal`で点灯して止まる。以降アニメーションしない。

`prefers-reduced-motion: reduce` の場合は完成状態を即時表示。

---

## 2. 掲載内容と全コピー

> 実案件のクライアント名・依頼番号は出さない。業種＋規模の匿名表記に統一する。守秘の姿勢そのものが評価対象になる。

### ヘッダー

- 左: `sawada` ＋ 小さく `業務自動化 / Google Workspace / Web制作`
- 右: `● 受付中`（`--terminal`のドット）＋ `依頼する`（アンカー）

### ヒーロー

見出し:
> 手で繰り返している工程を、
> 1本の線に置き換えます。

リード（3行以内）:
> 通信インフラの構築現場で8年、プロジェクトリーダーとしてシステム導入と運用の自動化をやってきました。その手法を、そのまま中小規模の業務に持ち込みます。スプレッドシートの転記、メールの仕分け、集計と通知——動かし続ける前提で組みます。

稼働条件（ヒーロー内に置く。フッターに逃がさない）:
> 平日夜間と土日で対応しています。やり取りはチャットのみ、通話・訪問は行いません。

結線図のラベル（上段＝手作業）:
`受信` `転記` `突合` `集計` `作成` `通知`
下段のバイパス線ラベル: `自動化後`

### できること（3領域）

各領域: 見出し／2文の説明／具体タスク4つ

**1. 業務自動化（GAS・API連携）**
> Google Apps Scriptを中心に、いま人がやっている手順をそのまま置き換えます。動かしたあとに壊れないよう、エラー時の通知と復旧手順まで含めて納品します。
- Gmail受信 → 内容判定 → スプレッドシート記録
- 複数ブックの突合・集計・帳票出力
- フォーム受付から通知・台帳更新までの一気通貫
- 外部API（OpenAI / Gemini など）を挟んだ自動仕分け

**2. Google Workspace 運用・管理**
> 大規模なアカウント／端末管理で使ってきた運用設計を、そのまま縮小して提供します。組織の作り方と権限の切り方を最初に決めておくと、後の事故がほぼなくなります。
- 組織部門・グループ・共有ドライブの設計
- 端末（Chrome OS等）の一括登録とポリシー適用
- 外部共有・アクセス権限のトラブル調査と是正
- Directory APIによる棚卸し・台帳自動生成

**3. Web制作（LP・フォーム連携）**
> 見た目だけでなく、送信された内容がどこに溜まって誰に届くかまで作ります。サーバー不要の構成にして、運用費をかけずに保守できる形で渡します。
- ランディングページの設計・実装・公開
- フォーム → スプレッドシート → 通知の非同期連携
- 表示速度・アクセシビリティの担保
- 計測イベントの設計と設置

### 納めたもの（ケース3本）

各ケースの構成: `課題` / `やったこと` / `結果` の3行＋使用技術タグ

**Case 1 — 商業施設の出店テナント調査リスト**
- 課題: イベント協業の候補先を探すため、施設ごとの入居店舗を横断で集める必要があった
- やったこと: 対象施設の店舗情報を収集し、項目を揃えたスプレッドシートに整形して納品
- 結果: 納期内に検収完了。発注者から満足の評価を受領
- タグ: `データ収集` `スプレッドシート整形`

**Case 2 — 電子部材の仕様・調達先調査**
- 課題: 開発中デバイスに使う部材の候補と入手経路を短期間で洗い出したかった
- やったこと: 仕様条件に合致する部材を調査し、比較可能な形で一覧化
- 結果: 完遂。以降も継続して相談を受ける関係になった
- タグ: `技術調査` `比較整理`

**Case 3 — 勤怠管理システム（自社開発）**
- 課題: 打刻と集計が分かれていて、残業の超過に気づくのが月末になっていた
- やったこと: GASとHTML Serviceで打刻・有給申請・集計を1つに統合。36協定の超過を事前予測し、給与概算を自動でメール配信する仕組みを実装
- 結果: 月次の集計作業と、超過の見落としをなくした
- タグ: `Google Apps Script` `HTML Service` `自動メール配信`

### 公開デモ（2枚）

figure要素で大きめに。カードにしない。

見出しの下・2枚の上に、導入を1つ置く:

> 同じ作り方をどの案件にも当てはめてはいません。このページはJavaScriptを使わずに組み（計測時点のモバイルLighthouseはPerformance 98、他の3項目が100）、下の2本では動きと画面上の操作を実装しています。

書き方の制約:

- 数値は**実測値のみ**。「高速」「最高水準」などの形容は使わない
- スコアは計測条件で動くので `計測時点の` を必ず付け、断定しない
- 計測し直したら数値を更新する。**ローカルサーバーの値ではなく公開URLの実測値**を載せること（両者は一致しない）

計測の記録（2026-09-04、`tools/lighthouse-run.mjs 3 <URL> mobile` の中央値）:

| 対象 | Performance | Accessibility / Best Practices / SEO |
|---|---|---|
| 公開URL・1回目（導入文の追加前） | **98** | 100 |
| 公開URL・2回目（導入文の追加後） | **99** | 100 |
| ローカルサーバー | 99 | 100 |

**Performance は公開URLで 98〜99 の間を振れる。** 実ネットワーク越しの計測なので回線とCDNの状態で1点は動く（FCP 937〜2106ms、TBT 0〜342ms の散らばりを観測）。ページを重くした2回目の方が高く出ていることからも、差は改善ではなくノイズと判断できる。

そこで**掲載する数字は観測した下限の 98 に据え置き、計測のたびに追いかけない**。控えめな数字なら訪問者が自分で計測しても食い違わないが、上限を載せると下振れしたときに実際と合わなくなる。Accessibility / Best Practices / SEO は一貫して100なので、こちらは断定してよい。

数字を動かすのは、**下限が 98 を割ったとき**か、**構成を変えて明確に傾向が変わったとき**に限る。

「JavaScriptを使わずに組み」は事実として保つこと。`index.html` に `<script>`・インラインのイベントハンドラ・`javascript:` を一切入れない（`tools/check.mjs` が検査する）。

**株式会社クロステック・アーキテクツ（架空）**
> 中小企業向けの業務自動化コンサルを想定したBtoBサイト。導入効果を試算するスライダーと、サーバーを使わないフォーム送信を実装しています。
- 見どころ: ROIシミュレーター / Google Apps Script連携フォーム / ダークモード
- https://wada3333.github.io/crosstech-lp/

**STUDIO CORE（架空）**
> パーソナルジムを題材にした、申し込み導線重視のBtoC向けLP。空き枠の表示から申し込み完了までを1画面で完結させています。
- 見どころ: 予約フォームの空き枠連携 / 表示速度 / 計測イベント設計
- https://wada3333.github.io/studio-core-lp/

### 技術と資格

タグの羅列にしない。3つのグループに分けて、それぞれ1行の説明を付ける。

- **作るもの**: Google Apps Script / JavaScript (ES6+) / HTML5 / CSS3 / Python / Google Sheets API / Directory API / 各種LLM API
- **運用と設計**: Google Workspace管理 / ネットワーク構築 / 資産管理 / プロジェクト管理（PL歴8年）
- **資格**: Tableau Data Saber / CompTIA Project+ / Microsoft Azure Fundamentals (AZ-900) / 工事担任者 AI・DD総合種 / 電気通信工事施工管理 / ビジネス統計スペシャリスト（エクセル分析 上級）/ 日商簿記2級

### 進め方（5ステップ・番号を振ってよい唯一の箇所）

1. **相談** — 現在の手順を、実際の画面やファイルで見せてください。文章の説明より速く正確に把握できます。
2. **範囲を決める** — できること・できないこと・想定作業時間をこちらから提示します。ここで折り合わなければ辞退します。
3. **作る** — 進捗は区切りごとにチャットで共有します。動くものを早めに見せる進め方をとります。
4. **確認** — 実データで動かして確認していただきます。修正はこの段階に含みます。
5. **引き渡し** — 操作手順と、壊れたときの対処をドキュメントで渡します。

### 依頼する（CTA）

見出し:
> 手順を見せていただくところから始めます。

本文:
> 何を自動化できるか決まっていない段階でも構いません。いまの手順を見せていただければ、そもそも自動化すべきかどうかから一緒に判断します。

ボタン2つ（横並び）:
- `ランサーズで相談する` → `https://www.lancers.jp/profile/sawada3333`
- `クラウドワークスで相談する` → `https://crowdworks.jp/public/employees/7046598`

いずれも `target="_blank"` `rel="noopener noreferrer"` を付与する。

ボタン下の注記:
> 平日夜間・土日に対応します。チャットでのやり取りのみです。

### フッター

`sawada / 2026` と、GitHubへのリンク（https://github.com/wada3333）のみ。

---

## 3. 実装要件

- 単一の `index.html`（CSSは`<style>`に内包、JSは`<script>`に内包）。ビルド不要。
- GitHub Pagesで公開。ルートに配置。
- フォントはGoogle Fontsから読み込み、`font-display: swap`。
- 画像はデモサイトのスクリーンショット2枚のみ。WebP、`loading="lazy"`、明示的な`width`/`height`。
- Lighthouse: Performance / Accessibility / Best Practices / SEO すべて95以上。
- `prefers-color-scheme` には対応しない（このLPは1テーマで完結させる）。
- OGP: `og:title` `og:description` `og:image`（1200×630のOG画像も生成する）。
- フォーム・問い合わせ機能は載せない。導線は外部プロフィールへのリンクのみ。
- キーボードフォーカスは`--terminal`の2px実線アウトラインで明示する。
- 見出しは h1 → h2 → h3 の順序を崩さない。

### 検証項目

- [ ] 375px幅で横スクロールが発生しない
- [ ] 結線図が375px幅で破綻せず、縦積みに切り替わる
- [ ] ヒーロー見出しが文節の途中で改行しない（512px以上は2行、それ未満は3行）
- [ ] 767px以下で縦導線が表示され、h2・h3・本文の左端が揃っている
- [ ] `prefers-reduced-motion` でアニメーションが無効化される
- [ ] 全リンクがキーボードで到達・操作できる
- [ ] `--line` と `--paper` のコントラスト比が4.5:1以上
- [ ] 実クライアント名・依頼番号が1箇所も含まれていない
- [ ] 勤務先が特定されうる記述（企業名・グループ名・端末台数・導入先の業種）が1箇所も含まれていない

---

## 4. Claude Code に渡すプロンプト

```
このリポジトリに、静的なポートフォリオLPを1ファイル（index.html）で実装してください。
仕様は portfolio-lp-spec.md にすべて記載されています。まず全文を読んでから着手してください。

厳守事項:
- CSSとJSはindex.html内に内包。ビルドツールは使わない。
- 仕様書のカラートークン名・値をCSS変数としてそのまま定義する。
- コピーは仕様書の文言を一字一句そのまま使う。要約・言い換えをしない。
- 仕様書の「やらないこと」に挙げた表現を使わない。
- モーションはヒーローの結線図の1回のみ。他の要素にスクロール連動アニメーションを付けない。

進め方:
1. 仕様書を読み、不明点があれば実装前にまとめて質問する
2. index.html を実装する
3. 仕様書「検証項目」のチェックリストを1つずつ自分で確認し、結果を報告する
4. 375px / 768px / 1440px の3幅でスクリーンショットを撮り、崩れがないか自己レビューする

画像（デモ2サイトのスクリーンショット、OG画像）はまだ用意していません。
正しいサイズとalt属性を持つプレースホルダーを置き、必要なファイル名と寸法を一覧で教えてください。
```

---

## 5. 決定事項

- 表記名: `sawada`（ランサーズのアカウント名に揃える）
- CTAリンク先: ランサーズ `https://www.lancers.jp/profile/sawada3333` / クラウドワークス `https://crowdworks.jp/public/employees/7046598`
- 本業案件は**掲載しない**。勤務先および案件が特定されうるため、端末台数・導入先の業種・グループ名を一切書かない。経歴の記述は「通信インフラの構築でプロジェクトリーダー歴8年」の粒度に留める

### 残る作業

- [ ] デモ2サイトのスクリーンショット撮影（1440px幅・ファーストビュー、WebP変換）
- [ ] OG画像の作成（1200×630）
